/**
 * Created by ed on 30.11.14.
 */
define([
    'jquery',
    'underscore',
    'backbone',
    'localstorage'
], function(
    $,
    _,
    Backbone
){
   var DayModel = Backbone.Model.extend({

   });

    return DayModel;
});